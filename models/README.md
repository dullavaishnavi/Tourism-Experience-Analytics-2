# Models

`r.joblib` is the trained regression pipeline for attraction rating prediction.

`c.joblib` is the trained classification pipeline for visit-mode prediction.

Both models were trained with the scikit-learn version pinned in `requirements.txt`. Do not change the scikit-learn version without retraining or re-exporting the models.
